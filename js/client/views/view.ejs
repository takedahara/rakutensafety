<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body, html {
            height: 100%; /* 画面全体の高さを100%に設定 */
            margin: 0;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #f0f0f0;
            overflow: hidden; /* 画面外にコンテンツが溢れるのを防ぐ */
        }

        .container {
            width: 100%; /* コンテナの幅を調整 */
            max-width: 2060px; /* 最大幅を設定 */
            height: 100%; /* コンテナの高さを100%に設定 */
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            display: flex;
            flex-direction: column;
            background: white;
        }

        .map {
            flex: 1;
            background: url('https://serai.jp/wp-content/uploads/2022/02/06-640x364.jpg') no-repeat center center;
            background-size: cover; /* 背景画像をカバーとして設定 */
            height: 100%; /* 背景を画面下まで広げる */
        }

        .button-container {
            padding: 10px;
            overflow: auto; /* ボタンが多すぎる場合はスクロール可能にする */
        }

        .button {
            width: 100%;
            padding: 90px 10px; /* 上下のパディングを大幅に増やす */
            margin: 20px 0; /* マージンを適宜調整 */
            background-color: white;
            border: none;
            border-radius: 40px; /* 角を丸くする */
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            cursor: pointer;
            font-size: 16px;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="map"></div>
        <div class="button-container">
            <form action="/submit" method="POST">
                <button type="submit" name="action" value="map" class="button">地図へ</button>
                <button type="submit" name="action" value="healthcare" class="button">ヘルスケアへ</button>
                <button type="submit" name="action" value="safety" class="button">安全確認へ</button>
            </form>
        </div>
    </div>
</body>
</html>
