<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>サンプルページ</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body, html {
            height: 100vh;
            margin: 0;
            font-family: 'Roboto', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #f0f0f0;
            overflow: hidden;
        }

        .container {
            width: 100%;
            height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .map {
            position: absolute;
            width: 60vh;
            height: 177.78vh;
            max-width: 100vw;
            background: url('https://i.ytimg.com/vi/mE9SiIdwouw/maxresdefault.jpg') no-repeat center center;
            background-size: cover;
            z-index: 1;
            filter: brightness(0.7);
        }

        .button-container {
            position: relative;
            width: 85%; /* Adjusted width */
            z-index: 2;
            padding: 25px; /* Adjusted padding */
            background: rgb(207, 207, 207);
            border-radius: 50px; /* Adjusted border-radius */
            box-shadow: 0 2px 5px rgba(0,0,0,0.3);
            animation: fadeIn 1s ease-out; /* Fade-in and slide-up animation */
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px); /* Start slightly lower */
            }
            to {
                opacity: 1;
                transform: translateY(0); /* Slide up to original position */
            }
        }

        .button {
            width: 100%;
            padding: 30px 5px; /* Adjusted padding */
            margin: 20px 0; /* Adjusted margin */
            background-color: white;
            border: none;
            border-radius: 22px; /* Adjusted border-radius */
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            cursor: pointer;
            font-size: 32px; /* Adjusted font size */
            font-weight: bold;
            text-align: center;
            transition: all 0.3s ease;
        }

        .button:hover {
            background-color: #f0f0f0;
            transform: translateY(-3px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.2);
        }

        @media (orientation: portrait) {
            .map {
                width: 100vw;
                height: 56.25vw;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="map"></div>
        <div class="button-container">
            <form action="/Button2Map" method="POST">
                <button type="submit" name="action" value="map" class="button">地図へ</button>
            </form>
            <form action="/Button2HealthCare" method="POST">
                <button type="submit" name="action" value="healthcare" class="button">ヘルスケアへ</button>
            </form>
            <form action="/Button2Mobile" method="POST">
                <button type="submit" name="action" value="safety" class="button">安全確認へ</button>
            </form>
        </div>
    </div>
</body>
</html>
