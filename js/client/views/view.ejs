<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>楽天セーフティ | ホーム</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
      * {
        box-sizing: border-box;
      }

      *:focus {
        outline: none;
      }

      body {
        font-family: 'Roboto', sans-serif;
        background-color: #FFF;
        padding: 0;
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        overflow: hidden;
      }

      .background {
        background-color: #BF0000;
        padding: 40px 20px;
        width: 100%;
        max-width: 530px;
        min-height: 100vh;
        position: relative; /* For positioning the logout button */
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .button-container {
        margin: 0 auto;
        width: 100%;
        max-width: 480px;
        background-color: #FFF;
        padding: 30px;
        border-radius: 40px;
        text-align: center;
      }

      .button {
        border: 4px solid #BF0000;
        background: #FFF;
        color: #BF0000;
        font-size: 1.2em;
        line-height: 30px;
        padding: 15px 0;
        text-decoration: none;
        text-shadow: none;
        border-radius: 25px;
        box-shadow: none;
        transition: 0.25s;
        display: block;
        width: 100%;
        margin: 20px auto;
        font-weight: bold;
        cursor: pointer;
      }

      .button:hover {
        background-color: #f0f0f0;
        transform: translateY(-3px);
      }

      .logo {
        width: 300px;
        height: auto;
        margin-bottom: 20px;
      }

      .back-button {
        position: absolute;
        bottom: 20px; /* Align at the bottom with a margin */
        left: 50%;
        transform: translateX(-50%);
        padding: 15px 30px;
        font-size: 1.2em;
        font-weight: 600; /* Slightly bolder */
        background-color: #FFF;
        color: #BF0000;
        border: 4px solid #BF0000;
        border-radius: 25px;
        cursor: pointer;
        transition: 0.25s;
      }

      .back-button:hover {
        background-color: #f0f0f0;
        transform: translate(-50%, -3px);
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
</head>
<body>
  <script>
    function goBack() {
      // Perform the desired action, e.g., navigating back
      window.history.back();
    }
  </script>
  <div class="background">
    <div class="button-container">
      <img src="http://localhost:5500/js/client/image/safety_log.jpg" alt="Logo" class="logo">
      <!-- Each form has its own action but shares the same button style -->
      <form action="/Map" method="get">
        <button type="submit" name="action" value="map" class="button">地図へ</button>
      </form>
      <form action="https://pharmacy.healthcare.rakuten.co.jp/" method="get" target="_blank">
        <button type="submit" name="action" value="healthcare" class="button">ヘルスケアへ</button>
      </form>
      <form action="https://network.mobile.rakuten.co.jp/info/disaster/" method="get" target="_blank">
        <button type="submit" name="action" value="safety" class="button">安否確認へ</button>
      </form>
    </div>
    <!-- Logout Button placed at the bottom -->
    <button onclick="goBack()" class="back-button">ログアウト</button>
  </div>
</body>
</html>
